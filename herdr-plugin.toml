id = "ryonakae.agent-context"
name = "Agent Context"
version = "0.4.0"
min_herdr_version = "0.8.0"
description = "Display Pi, Claude Code, Codex, and OpenCode session context with optional tab and pane labels."
platforms = ["macos", "linux"]

[[startup]]
command = ["bin/herdr-agent-context", "listen"]

[[build]]
command = ["sh", "scripts/install-binary.sh"]
